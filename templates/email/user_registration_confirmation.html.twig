<h1>Hello {{ name }}!</h1>

<p>You are just one click away from creating your account</p>

<p>
    <a href="{{ url('api_registration', { token: token }) }}">Click here to confirm your account</a>
    <small>(This link will no longer be valid as of {{ expirationDate|date('d/m/Y a H:i') }})</small>
</p>
